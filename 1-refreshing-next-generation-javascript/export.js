const person = {
  name: 'Gustavo'
}

export default person

// export default (works for export all the content of the file)

//This way of export is called named exports (export only the data we want)
// export const clean = () => {...}
// export const baseData = 10
